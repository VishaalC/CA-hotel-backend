# CA-Hotel-Backend

## Steps to run
1. Add .env file containing the following details (PORT, MONGO_CONNECTION_URL, BCRYPT_SALT_ROUNDS, JWT_SECRET_KEY)\
2. Run `npm install`
3. Run `npm start:dev`

## Assumptions made
1. Considered username to be unique feature of the admin entity.
